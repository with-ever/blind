plugins {
    id 'java'
//    id "com.ewerk.gradle.plugins.querydsl" version "1.0.9"
    id 'org.springframework.boot' version '2.0.6.RELEASE'
}

//apply plugin: 'com.ewerk.gradle.plugins.querydsl'

allprojects {
    apply plugin: 'java'
    apply plugin: 'eclipse'
    apply plugin: 'org.springframework.boot'
    apply plugin: 'io.spring.dependency-management'

    sourceCompatibility = 1.8

    repositories {
        jcenter()
        mavenCentral().artifactUrls("http://repository.jboss.org/nexus/content/groups/public-jboss")
        maven { url "http://repo1.maven.org/maven2/" }
        maven { url "http://nexus.nhnent.com/content/groups/public" }
        maven { url "http://nexus.nhnent.com/content/repositories/releases/" }
        maven { url "https://repo.spring.io/libs-release" }
        maven { url "https://repo.spring.io/libs-milestone" }
        maven { url "https://oss.sonatype.org/content/repositories/snapshots" }
    }

    dependencies {
        // JUnit 5
        // testCompile group: 'junit', name: 'junit', version: '4.12'
        testCompile("org.junit.jupiter:junit-jupiter-api:5.1.0")
        testRuntime("org.junit.jupiter:junit-jupiter-engine:5.1.0")
        testRuntime("org.junit.vintage:junit-vintage-engine:5.1.0")

        compile "javax.xml.bind:jaxb-api:2.2.+"
        //롬복 설정
        compile "org.projectlombok:lombok:1.18.2"

        testCompile "org.springframework.boot:spring-boot-starter-test"
        testCompile "org.springframework.boot:spring-boot-starter-web"
    }
}

configure([project(':api'), project(':admin')]) {
    apply plugin: 'war'
    jar.enabled = false
}

bootJar.enabled = false

